import { formInitialState } from "./state"

export const reducerInputChange = (e: React.ChangeEvent<HTMLInputElement>, dispatchFormValue: any) => {
    // const { name, value } = e.target
    // const error = formInitialState[name].error
    // const text = formInitialState[name].text
    // const valid = e.target.checkValidity()

    // dispatchFormValue({
    //     [name]: {
    //         error,
    //         text,
    //         valid,
    //         value,
    //     }
    // })
}